# Reverse-engineering Dunedin bus tracking
(with AI)

## Running this project
This project uses `uv` for package management.

Run the `dunedin_bus_tracker.py` script to test out the REST API endpoints, and add the `--websocket` flag to test live tracking functionality.
